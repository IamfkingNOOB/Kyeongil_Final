using System.Collections.Generic;
using UnityEngine;

public class PlayerQTE : MonoBehaviour
{
    // Structure: User selects 3 characters at most.
    // in those, the leader starts at first.
    // rest are in List.
    // 

    private List<BasePlayerController> players = new List<BasePlayerController>();

    
}
